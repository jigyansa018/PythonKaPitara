name = "Isha is a good  girl and"
print(name.replace(" "," "))
 #replace double string
#  name doesn't change it  will generate a new string
# so strings are immutable in python which mean  that you cannot 
# change them by running function them  